To Run the program setup the createData.cfg file

Make a folder called "3DModel" 
In that folder save the 3D models .obj or .ply

Make a folder called "BackgroundImages"
Where all background images that is needed will be stored.

Run the "RenderAndAnnotationYolo.py" script and it will generated an "Output" folder containing the images and annotatied .txt files. 
